how to start?
==========================
- go to the folder where server.js stays from DOS command window
- run the following command
	npm install    ---- make sure node_modules are downloaded
	node server.js

